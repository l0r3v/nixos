name: Nix Quality Checks

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    name: Lint & Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v25
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Check Flake Syntax (Statix)
        run: nix shell nixpkgs#alejandra -c alejandra --check .

      - name: Find Dead Code (Deadnix)
        run: nix shell nixpkgs#deadnix -c deadnix .

      - name: Flake Check
        run: nix flake check
